snooze-till-graph-age-minutes: 120
prefix:
  schema: <https://schema.org/>
assert:
  - subjects: 
    SPARQL: >
      SELECT ?s
      WHERE {
        ?s ?p schema:Project .
      }
    paths: 
      - "<https://schema.org/Dataset>"