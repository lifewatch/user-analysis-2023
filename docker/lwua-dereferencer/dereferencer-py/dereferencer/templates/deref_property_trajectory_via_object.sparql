{#
    This Template is used to make a SPARQL query that will return the subjects of a given property trajectory.
    The property trajectory is defined by an object and a property_trajectory.
    This list will be concatenated by /
    This template takes in 2 parameters:
        - "object" : The URI of the property
        - "property_trajectory" : A list of the URIs of the subjects of the trajectory
#}

{% set property_path = property_trajectory|join('>/<') %}

SELECT ?s
WHERE {
        ?s  <{{ property_path }}> <{{ object }}> .
    }